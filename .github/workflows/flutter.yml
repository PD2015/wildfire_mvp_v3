name: Flutter CI/CD

on:
  push:
    branches: [ main, develop, 'feature/*', '**-a*-**' ]
  pull_request:
    branches: [ main, develop ]

jobs:
  # Core quality gates - must pass before any deployment
  build:
    name: Quality Gates (Format, Analyze, Test)
    runs-on: ubuntu-latest
    timeout-minutes: 15
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Java (for Android toolchain)
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.35.5'
          channel: 'stable'
          cache: true

      - name: Cache pub dependencies
        uses: actions/cache@v3
        with:
          path: |
            ${{ env.PUB_CACHE }}
            ~/.pub-cache
          key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-pub-

      - name: Get dependencies
        run: flutter pub get

      - name: Format check (C1)
        run: |
          dart format --output=none --set-exit-if-changed .

      - name: Analyze (C1)
        run: flutter analyze

      - name: Run tests (C1, C5)
        run: flutter test --platform=chrome --reporter expanded

      - name: LocationResolver tests (A4 validation)
        run: |
          echo "Running A4 LocationResolver test suite..."
          flutter test test/unit/services/location_resolver_test.dart --platform=chrome
          flutter test test/widget/manual_location_dialog_test.dart --platform=chrome
          flutter test test/integration/location_flow_test.dart --platform=chrome

      - name: Secret scan with gitleaks (C2)
        uses: gitleaks/gitleaks-action@v2
        with:
          args: detect --source . --no-git -v

      - name: Optional color/palette guard (C4)
        shell: bash
        run: |
          if [ -x scripts/color_guard.sh ]; then
            scripts/color_guard.sh
          else
            echo "No color_guard.sh found; skipping palette check.";
          fi

      - name: Upload test results (always)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: |
            **/coverage/**
            **/test/**/golden/**

  # Constitutional compliance checks
  constitutional-compliance:
    name: Constitutional Compliance (C1-C5)
    runs-on: ubuntu-latest
    needs: [build]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Flutter
      uses: subosito/flutter-action@v2
      with:
        flutter-version: '3.35.5'
        channel: 'stable'
        cache: true
        
    - name: Cache pub dependencies
      uses: actions/cache@v3
      with:
        path: |
          ${{ env.PUB_CACHE }}
          ~/.pub-cache
        key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.lock') }}
        restore-keys: |
          ${{ runner.os }}-pub-
          
    - name: Get dependencies
      run: flutter pub get
      
    - name: Check for hardcoded secrets (C2 compliance)
      run: |
        if grep -R --include="*.dart" -E "(api_key|apiKey|API_KEY|secret|Secret|SECRET|password|Password|PASSWORD|token|Token|TOKEN).*=.*['\"]" lib/; then
          echo "❌ C2 FAIL: Hardcoded secret detected"
          exit 1
        else
          echo "✅ C2 PASS: No hardcoded secrets found"
        fi
      
    - name: Verify accessibility compliance (C3)
      run: |
        echo "🔍 Checking for accessibility compliance..."
        
        # Check for semantic labels in widgets
        if ! grep -r "Semantics\|semanticLabel\|excludeSemantics" lib/ > /dev/null; then
          echo "⚠️  C3 WARNING: No semantic labels found, verify accessibility"
        else
          echo "✅ C3 PASS: Semantic labels detected"
        fi
        
        # Check for proper touch target sizes
        if ! grep -r "kMinInteractiveDimension\|44\|48" test/ > /dev/null; then
          echo "⚠️  C3 WARNING: No touch target size tests found"
        else
          echo "✅ C3 PASS: Touch target size tests detected"
        fi
        
    - name: Verify transparency compliance (C4)
      run: |
        echo "🔍 Checking for transparency compliance..."
        
        # Check for timestamp display
        if ! grep -r "Updated\|Cached\|timestamp\|observedAt" lib/ > /dev/null; then
          echo "❌ C4 FAIL: No timestamp/freshness indicators found"
          exit 1
        else
          echo "✅ C4 PASS: Timestamp indicators found"
        fi
        
        # Check for source labeling
        if ! grep -r "source\|Source\|EFFIS\|SEPA\|Mock" lib/ > /dev/null; then
          echo "❌ C4 FAIL: No data source labeling found"
          exit 1
        else
          echo "✅ C4 PASS: Data source labeling found"
        fi
        
    - name: Verify resilience compliance (C5)
      run: |
        echo "🔍 Checking for resilience compliance..."
        
        # Check for error handling
        if ! grep -r "try\|catch\|Either\|Left\|Right" lib/ > /dev/null; then
          echo "❌ C5 FAIL: No error handling patterns found"
          exit 1
        else
          echo "✅ C5 PASS: Error handling patterns found"
        fi
        
        # Check for retry mechanisms
        if ! grep -r "retry\|Retry\|fallback\|Fallback" lib/ > /dev/null; then
          echo "❌ C5 FAIL: No retry/fallback mechanisms found"
          exit 1
        else
          echo "✅ C5 PASS: Retry/fallback mechanisms found"
        fi

  # Web build with API key injection (A11)
  build-web:
    name: Build Web Artifact
    needs: [build, constitutional-compliance]
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
      
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.35.5'
          channel: 'stable'
      
      - name: Get dependencies
        run: flutter pub get
      
      - name: Build web with API key injection
        env:
          MAPS_API_KEY_WEB: ${{ secrets.GOOGLE_MAPS_API_KEY_WEB_PREVIEW }}
        run: ./scripts/build_web_ci.sh
      
      - name: Upload web artifact
        uses: actions/upload-artifact@v4
        with:
          name: web-build
          path: build/web/
          retention-days: 7

  # Deploy to Firebase preview channel for PRs (A11)
  deploy-preview:
    name: Deploy Preview Channel
    needs: build-web
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      
      - name: Download web artifact
        uses: actions/download-artifact@v4
        with:
          name: web-build
          path: build/web/
      
      - name: Deploy to Firebase preview channel
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}'
          projectId: '${{ secrets.FIREBASE_PROJECT_ID }}'
          channelId: 'pr-${{ github.event.pull_request.number }}'
          expires: 7d

  # Deploy to production (requires manual approval) (A11)
  deploy-production:
    name: Deploy Production
    needs: build-web
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment:
      name: production
      url: https://wildfire-app-e11f8.web.app
    steps:
      - uses: actions/checkout@v4
      
      - name: Download web artifact
        uses: actions/download-artifact@v4
        with:
          name: web-build
          path: build/web/
      
      - name: Deploy to Firebase production
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT }}'
          projectId: '${{ secrets.FIREBASE_PROJECT_ID }}'
          channelId: live
